# 🌍 دليل الترجمة ودعم اللغات المتعددة (i18n)

## نظرة عامة

تم تحديث **يمن بيديا** ليدعم **20 لغة عالمية** باستخدام مكتبة i18next!

---

## 🗣️ اللغات المدعومة (20 لغة)

| اللغة | الكود | الاسم المحلي | الأيقونة |
|------|-------|-------------|----------|
| العربية | `ar` | العربية | 🇾🇪 |
| الإنجليزية | `en` | English | 🇬🇧 |
| الفرنسية | `fr` | Français | 🇫🇷 |
| الإسبانية | `es` | Español | 🇪🇸 |
| الألمانية | `de` | Deutsch | 🇩🇪 |
| الإيطالية | `it` | Italiano | 🇮🇹 |
| البرتغالية | `pt` | Português | 🇵🇹 |
| الروسية | `ru` | Русский | 🇷🇺 |
| الصينية | `zh` | 中文 | 🇨🇳 |
| اليابانية | `ja` | 日本語 | 🇯🇵 |
| الكورية | `ko` | 한국어 | 🇰🇷 |
| الهندية | `hi` | हिन्दी | 🇮🇳 |
| البنغالية | `bn` | বাংলা | 🇧🇩 |
| الأردية | `ur` | اردو | 🇵🇰 |
| التركية | `tr` | Türkçe | 🇹🇷 |
| الفارسية | `fa` | فارسی | 🇮🇷 |
| الإندونيسية | `id` | Bahasa Indonesia | 🇮🇩 |
| الماليزية | `ms` | Bahasa Melayu | 🇲🇾 |
| السواحيلية | `sw` | Kiswahili | 🇰🇪 |
| الهولندية | `nl` | Nederlands | 🇳🇱 |

---

## 📁 الهيكل

```
frontend/
├── src/
│   ├── i18n/
│   │   ├── config.js                    # إعدادات i18next
│   │   ├── generate-translations.js     # سكريبت توليد الترجمات
│   │   └── translations/                # ملفات الترجمة
│   │       ├── ar.json                  # العربية (كامل)
│   │       ├── en.json                  # الإنجليزية (كامل)
│   │       ├── fr.json                  # الفرنسية (كامل)
│   │       ├── es.json                  # الإسبانية
│   │       ├── de.json                  # الألمانية
│   │       ├── it.json                  # الإيطالية
│   │       ├── pt.json                  # البرتغالية
│   │       ├── ru.json                  # الروسية
│   │       ├── zh.json                  # الصينية
│   │       ├── ja.json                  # اليابانية
│   │       ├── ko.json                  # الكورية
│   │       ├── hi.json                  # الهندية
│   │       ├── bn.json                  # البنغالية
│   │       ├── ur.json                  # الأردية
│   │       ├── tr.json                  # التركية
│   │       ├── fa.json                  # الفارسية
│   │       ├── id.json                  # الإندونيسية
│   │       ├── ms.json                  # الماليزية
│   │       ├── sw.json                  # السواحيلية
│   │       └── nl.json                  # الهولندية
│   ├── components/
│   │   └── LanguageSwitcher.js          # مكون تبديل اللغة
│   └── index.js                         # تفعيل i18n
```

---

## 🚀 الاستخدام

### 1. في المكونات (Components)

```javascript
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t, i18n } = useTranslation();

  return (
    <div>
      <h1>{t('home.welcome')}</h1>
      <p>{t('home.subtitle')}</p>

      {/* اللغة الحالية */}
      <p>Current language: {i18n.language}</p>
    </div>
  );
}
```

### 2. تبديل اللغة

```javascript
import { useTranslation } from 'react-i18next';

function LanguageButton() {
  const { i18n } = useTranslation();

  const changeTo English = () => {
    i18n.changeLanguage('en');
  };

  return (
    <button onClick={changeToEnglish}>
      Switch to English
    </button>
  );
}
```

### 3. استخدام مكون Language Switcher

```javascript
import LanguageSwitcher from './components/LanguageSwitcher';

function Navbar() {
  return (
    <nav>
      <LanguageSwitcher />
    </nav>
  );
}
```

---

## 🎯 الميزات

### ✅ الميزات المطبقة

1. **20 لغة عالمية** - دعم كامل لـ 20 لغة
2. **تبديل اللغة الفوري** - تغيير اللغة دون إعادة تحميل
3. **دعم RTL/LTR تلقائي** - العربية، الأردية، الفارسية (RTL)
4. **حفظ اللغة محلياً** - تذكر اختيار اللغة في localStorage
5. **كشف اللغة تلقائياً** - يكتشف لغة المتصفح تلقائياً
6. **مكون Language Switcher** - قائمة منسدلة جميلة مع أعلام الدول
7. **نموذج Article محدث** - دعم المحتوى متعدد اللغات

### 📋 الميزات المخططة

- [ ] ترجمة المقالات تلقائياً باستخدام AI
- [ ] واجهة ترجمة للمحررين
- [ ] عرض المقالات بلغات متعددة
- [ ] بحث متعدد اللغات

---

## 📝 هيكل ملف الترجمة

كل ملف ترجمة (مثل `ar.json`) يحتوي على:

```json
{
  "siteName": "يمن بيديا",
  "siteDescription": "الموسوعة اليمنية الشاملة",

  "nav": {
    "home": "الرئيسية",
    "articles": "المقالات",
    "categories": "التصنيفات",
    "login": "تسجيل الدخول",
    "language": "اللغة"
  },

  "home": {
    "welcome": "مرحباً بك في يمن بيديا",
    "subtitle": "موسوعة يمنية شاملة",
    "latestArticles": "أحدث المقالات"
  },

  "article": {
    "readMore": "اقرأ المزيد",
    "views": "مشاهدات",
    "author": "الكاتب"
  },

  "common": {
    "save": "حفظ",
    "cancel": "إلغاء",
    "delete": "حذف",
    "loading": "جاري التحميل..."
  }
}
```

---

## 🔧 كيفية إضافة لغة جديدة

### 1. إنشاء ملف الترجمة

```bash
cd frontend/src/i18n/translations
# إنشاء ملف جديد، مثلاً للإيطالية
cp en.json it.json
# عدّل محتوى it.json للإيطالية
```

### 2. تحديث config.js

```javascript
// frontend/src/i18n/config.js
import it from './translations/it.json';

const resources = {
  ar: { translation: ar },
  en: { translation: en },
  it: { translation: it },  // إضافة
  // ...
};
```

### 3. تحديث LanguageSwitcher.js

```javascript
const languages = [
  // ...
  { code: 'it', name: 'Italian', nativeName: 'Italiano', flag: '🇮🇹', rtl: false },
  // ...
];
```

### 4. تحديث نموذج Article (Backend)

```javascript
// backend/models/Article.js
language: {
  type: String,
  default: 'ar',
  enum: ['ar', 'en', 'fr', ..., 'it']  // إضافة it
}
```

---

## 🛠️ أوامر مفيدة

### توليد ملفات ترجمة تلقائياً

```bash
cd frontend/src/i18n
node generate-translations.js
```

هذا السكريبت يُنشئ ملفات الترجمة للغات التي لا تملك ملفات بعد.

---

## 🌐 دعم RTL/LTR

اللغات التي تستخدم RTL (من اليمين إلى اليسار):
- العربية (`ar`)
- الأردية (`ur`)
- الفارسية (`fa`)

الكود يُطبّق RTL تلقائياً:

```javascript
// frontend/src/components/LanguageSwitcher.js
useEffect(() => {
  const lang = languages.find(l => l.code === i18n.language);
  document.documentElement.dir = lang?.rtl ? 'rtl' : 'ltr';
  document.documentElement.lang = i18n.language;
}, [i18n.language]);
```

---

## 📚 مصادر إضافية

### روابط مفيدة:
- [i18next Documentation](https://www.i18next.com/)
- [react-i18next Documentation](https://react.i18next.com/)
- [Language Codes (ISO 639-1)](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes)

### أمثلة على الاستخدام:

**في JSX:**
```jsx
<h1>{t('home.welcome')}</h1>
<button>{t('common.save')}</button>
```

**مع متغيرات:**
```jsx
// في ar.json: "greeting": "مرحباً {{name}}"
<p>{t('greeting', { name: 'أحمد' })}</p>
// النتيجة: مرحباً أحمد
```

**الجمع (Plurals):**
```json
// في ar.json
{
  "articles": "مقالة واحدة",
  "articles_plural": "{{count}} مقالات"
}
```

```jsx
<p>{t('articles', { count: 5 })}</p>
// النتيجة: 5 مقالات
```

---

## 🎨 التخصيص

### تغيير اللغة الافتراضية

```javascript
// frontend/src/i18n/config.js
i18n.init({
  fallbackLng: 'en',  // غير هذا من 'ar' إلى 'en' للإنجليزية
  // ...
});
```

### تعطيل الكشف التلقائي للغة

```javascript
// frontend/src/i18n/config.js
i18n
  // .use(LanguageDetector)  // علّق هذا السطر
  .use(initReactI18next)
  .init({
    // ...
  });
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الترجمات لا تظهر

**الحل:**
1. تحقق من أن `import './i18n/config'` موجود في `index.js`
2. تحقق من وجود ملف الترجمة في `translations/`
3. تأكد من أن المفتاح موجود في ملف JSON

### المشكلة: RTL لا يعمل

**الحل:**
1. تحقق من أن اللغة معرّفة كـ `rtl: true` في `LanguageSwitcher.js`
2. تأكد من أن Tailwind CSS يدعم RTL (`space-x-reverse`)

### المشكلة: اللغة لا تُحفظ

**الحل:**
تحقق من أن `LanguageDetector` يستخدم localStorage:

```javascript
detection: {
  order: ['localStorage', 'navigator', 'htmlTag'],
  caches: ['localStorage'],
}
```

---

## 📊 الإحصائيات

- **عدد اللغات**: 20 لغة
- **عدد المفاتيح**: ~150 مفتاح ترجمة
- **اللغات الكاملة**: العربية، الإنجليزية، الفرنسية
- **حجم الملفات**: ~5-10 KB لكل ملف JSON

---

## ✅ الخطوات التالية

1. ترجمة المحتوى الكامل لكل اللغات (حالياً الترجمة باللغة الإنجليزية كمرجع)
2. إضافة واجهة ترجمة في لوحة المحرر
3. دعم ترجمة المقالات باستخدام AI
4. إضافة مزيد من اللغات (50+ لغة)
5. تحسين محرك البحث ليدعم جميع اللغات

---

**تم التحديث:** نوفمبر 2025

**المطورون:** فريق يمن بيديا 🇾🇪

---

<div align="center">

**🌍 يمن بيديا متاحة الآن بـ 20 لغة عالمية! 🌍**

</div>
