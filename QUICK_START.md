# 🚀 دليل البدء السريع - يمن بيديا

## التثبيت في 5 دقائق

### 1️⃣ التحضيرات (قبل البدء)

**تأكد من تثبيت:**
- ✅ Node.js (الإصدار 18 أو أحدث)
- ✅ MongoDB (محلي أو MongoDB Atlas)
- ✅ محرر نصوص (VS Code مثلاً)

### 2️⃣ تشغيل MongoDB

**Windows:**
```bash
mongod
```

**Mac/Linux:**
```bash
sudo systemctl start mongod
```

**أو استخدم MongoDB Atlas** (مجاني):
- سجل في https://www.mongodb.com/cloud/atlas
- أنشئ Cluster مجاني
- احصل على رابط الاتصال

### 3️⃣ تثبيت المشروع

```bash
# انتقل لمجلد المشروع
cd yemen-pedia

# تثبيت Backend
cd backend
npm install

# إنشاء ملف .env
copy .env.example .env
```

### 4️⃣ إعداد ملف .env

افتح `backend/.env` وعدّل:

```env
# قاعدة البيانات
MONGODB_URI=mongodb://localhost:27017/yemenpedia

# مفتاح JWT (غيّره لمفتاح عشوائي!)
JWT_SECRET=my_super_secret_key_123456789

# مفتاح الذكاء الصناعي (احصل عليه من OpenAI)
AI_API_KEY=sk-...your_openai_key...
AI_API_PROVIDER=openai

# المنفذ
PORT=5000

# رابط Frontend
FRONTEND_URL=http://localhost:3000
```

**🔑 للحصول على مفتاح OpenAI:**
1. سجل في https://platform.openai.com/
2. اذهب إلى https://platform.openai.com/api-keys
3. أنشئ مفتاح جديد
4. انسخه في `AI_API_KEY`

### 5️⃣ تثبيت Frontend

```bash
# في نافذة جديدة
cd yemen-pedia/frontend
npm install

# إنشاء ملف .env
copy .env.example .env
```

### 6️⃣ إضافة التصنيفات اليمنية

```bash
cd ../database
node seed-categories.js
```

يجب أن ترى:
```
✅ Connected to MongoDB
✅ Inserted 15 categories
📜 التاريخ
🗺️ الجغرافيا
...
```

### 7️⃣ تشغيل المشروع

**نافذة 1 - Backend:**
```bash
cd backend
npm start
```

يجب أن ترى:
```
✅ Connected to MongoDB
🚀 Server running on port 5000
📚 يمن بيديا - Yemen-Pedia API
```

**نافذة 2 - Frontend:**
```bash
cd frontend
npm start
```

سيفتح المتصفح تلقائياً على: http://localhost:3000

## 🎉 تهانينا!

الموقع الآن يعمل! يمكنك:

### ✅ خطوات أولى:

1. **إنشاء حساب:**
   - اذهب إلى "إنشاء حساب"
   - املأ البيانات
   - سجل الدخول

2. **تحويل حسابك إلى Admin:**
   ```bash
   # افتح MongoDB Compass أو mongo shell
   # أو استخدم MongoDB Atlas

   db.users.updateOne(
     { email: "your_email@example.com" },
     { $set: { role: "admin" } }
   )
   ```

3. **جرّب المساعد الذكي:**
   - اذهب إلى "المساعد الذكي"
   - اسأل: "ما هي عاصمة اليمن؟"
   - جرّب أسئلة أخرى!

4. **أنشئ أول مقالة:**
   - اذهب إلى "مقالة جديدة"
   - اكتب عن موضوع يمني
   - انشرها!

## 🔧 أوامر مفيدة

### Backend
```bash
# تشغيل عادي
npm start

# تشغيل مع auto-restart
npm run dev
```

### Frontend
```bash
# تشغيل
npm start

# بناء للإنتاج
npm run build
```

### Database
```bash
# إعادة إضافة التصنيفات
cd database
node seed-categories.js
```

## ❓ حل المشاكل الشائعة

### ❌ خطأ: Cannot connect to MongoDB

**الحل:**
```bash
# تأكد من تشغيل MongoDB
mongod

# أو تحقق من رابط MongoDB في .env
```

### ❌ خطأ: AI API not working

**الحل:**
- تحقق من صحة `AI_API_KEY` في `.env`
- تأكد من وجود رصيد في حساب OpenAI
- جرّب تغيير `AI_API_PROVIDER` إلى `claude`

### ❌ خطأ: Port 5000 already in use

**الحل:**
```bash
# غيّر المنفذ في backend/.env
PORT=5001

# أو أوقف البرنامج الذي يستخدم المنفذ
```

### ❌ Frontend لا يتصل بـ Backend

**الحل:**
```bash
# تحقق من ملف frontend/.env
REACT_APP_API_URL=http://localhost:5000/api

# تأكد من أن Backend يعمل
```

## 📖 الخطوات التالية

1. ✅ اقرأ [الميزات الكاملة](FEATURES.md)
2. ✅ اطلع على [توثيق API](API_DOCS.md)
3. ✅ راجع [دليل التثبيت المفصل](INSTALLATION.md)
4. ✅ ابدأ المساهمة في المشروع!

## 🆘 تحتاج مساعدة؟

- 📧 افتح Issue على GitHub
- 💬 راسلنا على Discord (قريباً)
- 📚 راجع التوثيق الكامل

---

**استمتع ببناء يمن بيديا! 🇾🇪**
