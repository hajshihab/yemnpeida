# 🔐 دليل نظام الإدارة والصلاحيات - يمن بيديا

## 📋 نظرة عامة

تم تطوير نظام إدارة متقدم لموسوعة يمن بيديا يتضمن 4 مستويات من الصلاحيات مع لوحات تحكم متخصصة.

---

## 👥 مستويات الصلاحيات

### 1. **مستخدم عادي (User)**
- التسجيل والدخول
- قراءة المقالات
- كتابة مقالات (تحتاج موافقة)
- استخدام المساعد الذكي
- طلب أن يصبح محرراً

### 2. **محرر (Editor)**
- جميع صلاحيات المستخدم العادي
- الموافقة على المقالات ونشرها
- رفض المقالات
- جعل المقالات مميزة
- الوصول إلى لوحة المحرر

### 3. **مدير (Admin)**
- جميع صلاحيات المحرر
- الموافقة/رفض طلبات المحررين
- إدارة المستخدمين (عرض فقط)
- الوصول إلى لوحة الإدارة
- عرض الإحصائيات المتقدمة

### 4. **مدير رئيسي (Super Admin)** ⭐
- جميع صلاحيات المدير
- تغيير أدوار المستخدمين
- حذف المستخدمين
- لا يمكن حذفه أو تغيير دوره
- الوصول الكامل إلى النظام

---

## 🚀 البدء السريع

### 1. إنشاء المستخدم الرئيسي

تم إنشاء المستخدم الرئيسي تلقائياً:

```
Username: admin
Email: admin@yemenpedia.com
Password: Admin123!@#
```

**⚠️ مهم جداً:** قم بتغيير كلمة المرور فوراً بعد أول تسجيل دخول!

### 2. تسجيل الدخول

1. افتح الموقع: `http://localhost:3000`
2. اضغط "تسجيل الدخول"
3. أدخل البيانات أعلاه
4. سيتم توجيهك للصفحة الرئيسية

### 3. الوصول إلى لوحات التحكم

بعد تسجيل الدخول:
- **لوحة المحرر**: من القائمة العلوية → اسمك → لوحة المحرر
- **لوحة الإدارة**: من القائمة العلوية → اسمك → لوحة الإدارة

---

## 📊 لوحة التحكم الإدارية

### الوصول
- URL: `/super-admin`
- متاحة فقط لـ: Admin & Super Admin

### المميزات

#### 1. **إحصائيات شاملة**
- إجمالي المستخدمين
- عدد المحررين
- عدد المديرين
- طلبات المحررين المعلقة
- المستخدمين الجدد هذا الشهر

#### 2. **إدارة طلبات المحررين**
- عرض جميع الطلبات المعلقة
- الموافقة على الطلبات (يصبح المستخدم محرراً)
- رفض الطلبات مع إمكانية ذكر السبب
- عرض تاريخ تقديم الطلب

#### 3. **إدارة المستخدمين**
- عرض جميع المستخدمين
- البحث بالاسم أو البريد الإلكتروني
- فلترة حسب الدور (User/Editor/Admin)
- تغيير أدوار المستخدمين (Super Admin فقط)
- حذف المستخدمين (Super Admin فقط)
- عرض تاريخ التسجيل والمساهمات

---

## 📝 لوحة المحرر

### الوصول
- URL: `/admin`
- متاحة لـ: Editor, Admin, Super Admin

### المميزات

#### 1. **إحصائيات المقالات**
- إجمالي المقالات
- المقالات المنشورة
- المقالات المعلقة
- المسودات
- إجمالي المشاهدات
- عدد المحررين

#### 2. **إدارة المقالات**
- عرض جميع المقالات (حسب الحالة)
- الموافقة على المقالات ونشرها
- رفض المقالات مع ذكر السبب
- حذف المقالات
- جعل المقالات مميزة/إلغاء التمييز
- فلترة: الكل، معلقة، منشورة، مسودات، مرفوضة

---

## 🎯 سير العمل الكامل

### كيف يصبح المستخدم محرراً؟

```
1. المستخدم العادي
   ↓ [يسجل حسابه]

2. كتابة مقالات
   ↓ [يساهم بمقالات عالية الجودة]

3. طلب أن يصبح محرراً
   ↓ [من صفحة الملف الشخصي - زر "اطلب أن تصبح محرراً"]

4. مراجعة المدير
   ↓ [المدير يراجع الطلب في لوحة الإدارة]

5. الموافقة/الرفض
   ↓ [إذا تمت الموافقة]

6. محرر معتمد ✅
   [يحصل على صلاحيات المحرر]
```

### دورة حياة المقالة

```
1. مستخدم يكتب مقالة
   ↓ [حالة: draft/pending]

2. محرر يراجع المقالة
   ↓ [في لوحة المحرر]

3. قرار المحرر:

   أ) موافقة ✅
      ↓ [حالة: published]
      ↓ المقالة تظهر للجميع

   ب) رفض ❌
      ↓ [حالة: rejected]
      ↓ الكاتب يستلم إشعار بالسبب

   ج) تمييز ⭐
      ↓ [featured: true]
      ↓ تظهر في قسم المقالات المميزة
```

---

## 🔧 واجهة برمجة التطبيقات (API)

### نقاط النهاية الجديدة

#### إدارة المستخدمين

```javascript
// الحصول على جميع المستخدمين (Admin فقط)
GET /api/users
Query: ?role=editor&search=ahmed&page=1&limit=20

// الحصول على مستخدم محدد
GET /api/users/:id

// طلب أن يصبح محرراً (User)
POST /api/users/request-editor

// الموافقة/رفض طلب محرر (Admin فقط)
PUT /api/users/editor-request/:id
Body: { action: 'approve' | 'reject' }

// تغيير دور المستخدم (Super Admin فقط)
PUT /api/users/role/:id
Body: { role: 'user' | 'editor' | 'admin' }

// حذف مستخدم (Super Admin فقط)
DELETE /api/users/:id

// الحصول على طلبات المحررين المعلقة (Admin فقط)
GET /api/users/editor-requests/pending

// الحصول على إحصائيات (Admin فقط)
GET /api/users/stats/overview
```

### نموذج المستخدم المحدّث

```javascript
{
  username: String,
  email: String,
  password: String (hashed),
  displayName: String,
  role: 'user' | 'editor' | 'admin' | 'super_admin',
  editorStatus: 'pending' | 'approved' | 'rejected' | null,
  editorRequestDate: Date,
  editorApprovedBy: ObjectId (ref: User),
  bio: String,
  contributions: Number,
  verified: Boolean,
  avatar: String,
  createdAt: Date,
  updatedAt: Date
}
```

---

## 🎨 تحسينات الصفحة الرئيسية

### المميزات الجديدة

1. **Hero Section محسّن**
   - تصميم أكبر وأجمل مع gradient
   - 3 أزرار رئيسية: بحث، المساعد الذكي، ابدأ الكتابة
   - روابط سريعة للأقسام المهمة

2. **قسم المقالات المميزة**
   - عرض 3 مقالات مميزة
   - تصميم بطاقات جذاب مع hover effects
   - شارة "مميزة" على المقالات

3. **قسم التصنيفات المحسّن**
   - عرض 10 تصنيفات في الصفحة الرئيسية
   - تصميم أكبر وأوضح
   - وصف مختصر لكل تصنيف
   - hover effects جميلة

4. **قسم "كن محرراً"**
   - شرح خطوات أن تصبح محرراً
   - 3 خطوات بسيطة
   - زر مباشر للتسجيل

5. **قسم المساعد الذكي المحسّن**
   - تصميم أكبر وأكثر احترافية
   - شرح مميزات المساعد
   - 3 بطاقات للمميزات

---

## 🔐 الأمان والحماية

### Middleware للحماية

```javascript
// التحقق من أن المستخدم مدير
const isAdmin = (req, res, next) => {
  if (req.user.role !== 'admin' && req.user.role !== 'super_admin') {
    return res.status(403).json({ message: 'Access denied. Admin only.' });
  }
  next();
};

// التحقق من أن المستخدم super admin فقط
const isSuperAdmin = (req, res, next) => {
  if (req.user.role !== 'super_admin') {
    return res.status(403).json({ message: 'Access denied. Super Admin only.' });
  }
  next();
};
```

### قواعد الأمان

1. ✅ **لا يمكن حذف Super Admin**
2. ✅ **لا يمكن تغيير دور Super Admin إلا من نفسه**
3. ✅ **Admin لا يمكنه تغيير أدوار أو حذف Super Admin**
4. ✅ **جميع الطلبات الإدارية تتطلب JWT Token**
5. ✅ **Rate Limiting على جميع API endpoints**

---

## 📱 واجهة المستخدم

### الروابط الجديدة في Navbar

عند تسجيل الدخول، تظهر في القائمة المنسدلة:

- **لوحة المحرر** (للمحررين والمديرين)
  - أيقونة زرقاء
  - الوصول: Editor, Admin, Super Admin

- **لوحة الإدارة** (للمديرين فقط)
  - أيقونة أرجوانية
  - الوصول: Admin, Super Admin فقط

---

## 🎓 أمثلة الاستخدام

### مثال 1: إنشاء محرر جديد (من لوحة الإدارة)

```javascript
// 1. مستخدم يطلب أن يصبح محرراً
const requestEditor = async () => {
  const response = await axios.post(
    '/api/users/request-editor',
    {},
    { headers: { Authorization: `Bearer ${token}` } }
  );
};

// 2. المدير يوافق على الطلب
const approveEditor = async (userId) => {
  const response = await axios.put(
    `/api/users/editor-request/${userId}`,
    { action: 'approve' },
    { headers: { Authorization: `Bearer ${token}` } }
  );
};
```

### مثال 2: تغيير دور مستخدم (Super Admin فقط)

```javascript
const changeUserRole = async (userId, newRole) => {
  const response = await axios.put(
    `/api/users/role/${userId}`,
    { role: newRole },
    { headers: { Authorization: `Bearer ${token}` } }
  );
};
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا يمكن الوصول إلى لوحة الإدارة

**الحل:**
1. تأكد من أنك مسجل دخول
2. تحقق من أن دورك هو `admin` أو `super_admin`
3. تحقق من صلاحية JWT Token

### المشكلة: لا يظهر زر "اطلب أن تصبح محرراً"

**الحل:**
1. تأكد من أنك مسجل دخول
2. تحقق من أن دورك هو `user` فقط
3. تأكد من أنك لم تقدم طلباً من قبل

### المشكلة: لا أستطيع حذف مستخدم

**الحل:**
1. تأكد من أنك `super_admin`
2. لا يمكن حذف `super_admin`
3. تحقق من Console للأخطاء

---

## ✅ قائمة التحقق النهائية

- [x] إنشاء Super Admin
- [x] نظام الأدوار الأربعة يعمل
- [x] لوحة الإدارة كاملة
- [x] لوحة المحرر كاملة
- [x] نظام طلبات المحررين
- [x] الصفحة الرئيسية محسّنة
- [x] روابط Navbar محدّثة
- [x] API Routes كاملة
- [x] Middleware الأمان
- [x] MongoDB متصل بنجاح
- [x] التصنيفات والمصادر مضافة

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. راجع هذا الدليل
2. تحقق من Console للأخطاء
3. تأكد من أن Backend و Frontend يعملان
4. تحقق من MongoDB connection

---

## 🎉 تهانينا!

أصبح لديك الآن نظام إدارة متكامل ومتقدم لموسوعة يمن بيديا!

**صُنع بـ ❤️ من أجل اليمن 🇾🇪**
